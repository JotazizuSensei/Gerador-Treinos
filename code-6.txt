#eduModal');if(eduModal)eduModal.classList.remove('open');clone.querySelectorAll('input[type=file]').forEach(x=>x.value='');const boot=document.createElement('script');boot.textContent='window.__BLUE_BOOT_STATE__='+JSON.stringify(state).replace(/</g,'\\u003c')+';window.__BLUE_BOOT_VISUAL__='+JSON.stringify(visualCache).replace(/</g,'\\u003c')+';';const body=clone.querySelector('body');body.insertBefore(boot,body.firstChild);const blob=new Blob(['<!doctype html>\n'+clone.outerHTML],{type:'text/html'}),u=URL.createObjectURL(blob),a=document.createElement('a');a.href=u;a.download='BLUE_SYMBIOTE_v9_PORTABLE_COMPLETO.html';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(u),1000)}
async function importFullBackup(file){const r=new FileReader();r.onload=async()=>{try{const obj=JSON.parse(r.result);if(obj.state?.history&&obj.state?.prefs)state=obj.state;else if(obj.history&&obj.prefs)state=obj;else throw 0;ensureStateShape();if(obj.visualImages){await clearAllVisuals();for(const [k,v] of Object.entries(obj.visualImages))await putVisual(k,(v||[]).slice(0,2))}saveState();renderEduLibrary();renderVisualLibrary();renderBuild();renderFollow();alert('Backup importado: perfil, histórico, BLUE EDU e imagens disponíveis.')}catch(e){alert('Backup inválido.')}};r.readAsText(file)}

function saveClass(showAlert=true){if(!currentClass)return;const copy=JSON.parse(JSON.stringify(currentClass));const idx=state.history.findIndex(x=>x.id===currentClass.id);if(idx>=0)state.history[idx]=copy;else state.history.push(copy);saveState();requestPersistentStorage(false);if(showAlert)alert('Aula guardada.')}
function learning(){return Math.min(100,Math.round((state.history.length*8+state.feedback.length*12+Object.keys(state.prefs.favorite).length*3+Object.keys(state.prefs.avoid).length*3+Object.keys(state.eduOverrides||{}).length*2)/2))}
function renderHistory(){if(!$('historyList'))return;$('learnBadge').textContent='Perfil '+learning()+'%';$('profileInfo').innerHTML=`Aprendizagem: <b>${learning()}%</b> · aulas: <b>${state.history.length}</b> · feedbacks: <b>${state.feedback.length}</b> · favoritos: <b>${Object.keys(state.prefs.favorite).length}</b> · evitar: <b>${Object.keys(state.prefs.avoid).length}</b> · fichas EDU: <b>${Object.keys(state.eduOverrides||{}).length}</b>`;$('historyList').innerHTML=state.history.slice().reverse().map(h=>`<div class="history"><b>${esc(h.config.classType)} — ${h.config.duration} min</b><div class="small">${new Date(h.createdAt).toLocaleString('pt-PT')} · ${esc(h.mode)}</div><div class="row" style="margin-top:6px"><button class="btn" onclick="loadHistory('${h.id}')">ABRIR</button><button class="btn bad" onclick="deleteHistory('${h.id}')">APAGAR</button></div></div>`).join('')||'<div class="card note">Sem histórico.</div>'}
window.loadHistory=id=>{const h=state.history.find(x=>x.id===id);if(!h)return;currentClass=JSON.parse(JSON.stringify(h));finishClass();nav('build')};window.deleteHistory=id=>{state.history=state.history.filter(x=>x.id!==id);saveState()}
async function exportProfile(){return exportFullBackup()}
function importProfile(file){return importFullBackup(file)}
function openFeedback(){if(!currentClass)return;rating=0;$('feedbackModal').style.display='flex';document.querySelectorAll('#ratingRow button').forEach(b=>b.classList.remove('primary'))}
document.querySelectorAll('#ratingRow button').forEach(b=>b.onclick=()=>{rating=+b.dataset.rate;document.querySelectorAll('#ratingRow button').forEach(x=>x.classList.toggle('primary',x===b))});
function saveFeedback(){if(!rating)return alert('Escolhe nota 1–5.');const fb={id:currentClass.id,date:new Date().toISOString(),rating,flow:$('fbFlow').value,difficulty:$('fbDifficulty').value,variety:$('fbVariety').value,timing:$('fbTiming').value,notes:$('fbNotes').value.trim()};state.feedback.push(fb);const delta=(rating-3)*.35;currentClass.blocks.forEach(b=>b.exercises.forEach(ex=>state.prefs.exercise[canon(ex)]=(state.prefs.exercise[canon(ex)]||0)+delta));saveClass(false);saveState();$('feedbackModal').style.display='none';alert('Feedback assimilado.')}

$('findBtn').onclick=()=>{renderSuggestions();nav('suggest')};$('scratchBtn').onclick=scratch;$('scratchFromSuggest').onclick=scratch;$('saveBtn').onclick=saveClass;$('followBtn').onclick=()=>{currentStep=0;renderFollow();$('scrollMode').style.display='block';$('stepMode').style.display='none';nav('follow')};$('printBtn').onclick=()=>window.print();$('feedbackBtn').onclick=openFeedback;
$('stepEduBtn').onclick=()=>{const xs=flat();const ex=xs[currentStep];if(!ex)return;const parts=(ex.__num||'1.1').split('.').map(x=>Math.max(0,parseInt(x,10)-1));const original=currentClass?.blocks?.[parts[0]]?.exercises?.[parts[1]]||ex;openEdu(original,parts[0],parts[1])};
$('scrollModeBtn').onclick=()=>{$('scrollMode').style.display='block';$('stepMode').style.display='none';$('scrollModeBtn').classList.add('primary');$('stepModeBtn').classList.remove('primary');window.scrollTo({top:0,left:0,behavior:'auto'})};$('stepModeBtn').onclick=()=>{$('scrollMode').style.display='none';$('stepMode').style.display='block';$('stepModeBtn').classList.add('primary');$('scrollModeBtn').classList.remove('primary');renderStep();window.scrollTo({top:0,left:0,behavior:'auto'})};$('nextBtn').onclick=()=>{const xs=flat();if(currentStep<xs.length-1){currentStep++;renderStep();window.scrollTo({top:0,left:0,behavior:'auto'})}};$('prevBtn').onclick=()=>{if(currentStep>0){currentStep--;renderStep();window.scrollTo({top:0,left:0,behavior:'auto'})}};
$('closeEduBtn').onclick=closeEdu;$('saveEduBtn').onclick=saveEduOverride;$('resetEduBtn').onclick=resetEduOverride;$('eduModal').addEventListener('click',e=>{if(e.target===$('eduModal'))closeEdu()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&$('eduModal').classList.contains('open'))closeEdu()});
$('saveFeedback').onclick=saveFeedback;$('closeFeedback').onclick=()=>$('feedbackModal').style.display='none';$('exportBtn').onclick=exportProfile;$('importBtn').onclick=()=>$('importFile').click();$('importFile').onchange=e=>{const f=e.target.files?.[0];if(f)importProfile(f)};
fillEduFamilies();['eduSearch','eduFamily','eduCustom'].forEach(id=>$(id).addEventListener('input',renderEduLibrary));renderEduLibrary();
fillVisualFamilies();['visualSearch','visualFamily'].forEach(id=>$(id).addEventListener('input',renderVisualLibrary));$('exportVisualBtn').onclick=exportFullBackup;$('exportHtmlVisualBtn').onclick=exportStandaloneHtml;$('importVisualBtn').onclick=()=>$('importVisualFile').click();$('importVisualFile').onchange=e=>{const f=e.target.files?.[0];if(f)importFullBackup(f)};$('clearVisualBtn').onclick=async()=>{if(confirm('Apagar todas as imagens personalizadas da biblioteca visual?')){await clearAllVisuals();renderVisualLibrary();renderBuild();renderFollow()}};

function isStandaloneMode(){return window.matchMedia('(display-mode: standalone)').matches||window.navigator.standalone===true}
function isIOS(){return /iphone|ipad|ipod/i.test(navigator.userAgent)}
function setNetworkBadge(){const online=navigator.onLine;const b=$('networkBadge');if(!b)return;b.textContent=online?'ONLINE':'OFFLINE';b.classList.toggle('offline',!online)}
async function storageSnapshot(){let persisted=false,usage=0,quota=0;try{if(navigator.storage?.persisted)persisted=await navigator.storage.persisted();if(navigator.storage?.estimate){const e=await navigator.storage.estimate();usage=e.usage||0;quota=e.quota||0}}catch(e){}return{persisted,usage,quota}}
function humanBytes(n){if(!n)return'0 MB';return(n/1024/1024).toFixed(n>104857600?0:1)+' MB'}
async function updateDeviceStatus(){setNetworkBadge();const installed=isStandaloneMode();if($('installStatus'))$('installStatus').textContent=location.protocol==='file:'?'Portátil HTML':installed?'Instalada':'Browser';if($('offlineStatus'))$('offlineStatus').innerHTML=(navigator.serviceWorker&&location.protocol!=='file:')?'<span class="offlineReady">Preparada</span>':'<span class="portableReady">Modo portátil</span>';const s=await storageSnapshot();if($('storageStatus'))$('storageStatus').textContent=(s.persisted?'Protegido':'Normal')+(s.usage?' · '+humanBytes(s.usage):'');const top=$('installTopBtn'),main=$('installBtn');const shouldHide=installed||location.protocol==='file:';if(top)top.hidden=shouldHide;if(main)main.style.display=shouldHide?'none':'';if($('installHint')){if(location.protocol==='file:')$('installHint').textContent='Estás a usar a versão portátil. Para instalar como app, abre a versão alojada por HTTPS.';else if(installed)$('installHint').textContent='App instalada neste dispositivo. Pode ser aberta a partir do ecrã inicial/menu Iniciar.';else if(isIOS())$('installHint').textContent='No iPhone/iPad: abre no Safari → Partilhar → Adicionar ao ecrã principal.';else $('installHint').textContent='No Chrome/Edge/Android, usa INSTALAR APP quando o browser disponibilizar a instalação.'}}
async function requestPersistentStorage(showMessage=true){try{if(!navigator.storage?.persist){if(showMessage)alert('Este browser não expõe controlo de armazenamento persistente. O backup completo continua disponível.');return false}let ok=await navigator.storage.persisted();if(!ok)ok=await navigator.storage.persist();await updateDeviceStatus();if(showMessage)alert(ok?'Dados offline protegidos pelo browser.':'O browser não concedeu armazenamento persistente. Mantém backups completos regulares.');return ok}catch(e){if(showMessage)alert('Não foi possível alterar a proteção de armazenamento neste dispositivo.');return false}}
async function installApp(){if(isStandaloneMode())return;if(deferredInstallPrompt){deferredInstallPrompt.prompt();await deferredInstallPrompt.userChoice;deferredInstallPrompt=null;await updateDeviceStatus();return}if(isIOS()){alert('No iPhone/iPad: abre esta app no Safari, toca em Partilhar e escolhe “Adicionar ao ecrã principal”.');return}if(location.protocol==='file:'){alert('A instalação PWA requer a versão alojada por HTTPS. Esta cópia continua a funcionar como HTML portátil.');return}alert('Se o browser ainda não mostra a opção de instalação, usa o menu do Chrome/Edge → Instalar app / Adicionar ao ecrã principal.')}
window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredInstallPrompt=e;updateDeviceStatus()});window.addEventListener('appinstalled',()=>{deferredInstallPrompt=null;updateDeviceStatus();requestPersistentStorage(false)});window.addEventListener('online',updateDeviceStatus);window.addEventListener('offline',updateDeviceStatus);
async function registerPWA(){if(!('serviceWorker'in navigator)||location.protocol==='file:')return;try{swRegistration=await navigator.serviceWorker.register('./service-worker.js',{scope:'./'});if(swRegistration.waiting)showUpdateBox();swRegistration.addEventListener('updatefound',()=>{const w=swRegistration.installing;if(!w)return;w.addEventListener('statechange',()=>{if(w.state==='installed'&&navigator.serviceWorker.controller)showUpdateBox()})});navigator.serviceWorker.addEventListener('controllerchange',()=>location.reload())}catch(e){console.warn('Service worker não pôde ser registado.',e)}updateDeviceStatus()}
function showUpdateBox(){if($('updateBox'))$('updateBox').style.display='block'}
async function checkForUpdate(){if(!swRegistration){await registerPWA();if(!swRegistration)return alert('Atualização automática só está disponível na versão instalada/alojada.')}try{await swRegistration.update();if(swRegistration.waiting)showUpdateBox();else alert('Verificação concluída. Se existir uma nova versão, ficará disponível assim que o browser a instalar.')}catch(e){alert('Não foi possível procurar atualização agora.')}}
function applyUpdate(){if(swRegistration?.waiting)swRegistration.waiting.postMessage({type:'SKIP_WAITING'});else location.reload()}
if($('installTopBtn'))$('installTopBtn').onclick=installApp;if($('installBtn'))$('installBtn').onclick=installApp;if($('persistBtn'))$('persistBtn').onclick=()=>requestPersistentStorage(true);if($