Families(){const sel=$('eduFamily');if(!sel)return;[...new Set(APP.exercises.map(x=>x.family).filter(Boolean))].sort().forEach(v=>{const o=document.createElement('option');o.value=v;o.textContent=v;sel.appendChild(o)})}
function renderEduLibrary(){if(!$('eduList'))return;const q=lower($('eduSearch').value),fam=$('eduFamily').value,mode=$('eduCustom').value;let arr=uniqueCanonExercises().filter(ex=>(!fam||ex.family===fam)&&(!q||lower([ex.name,ex.region,ex.pattern,ex.objective,ex.family].join(' ')).includes(q)));if(mode==='custom')arr=arr.filter(ex=>!!state.eduOverrides[canon(ex)]);if(mode==='auto')arr=arr.filter(ex=>!state.eduOverrides[canon(ex)]);$('eduStats').textContent=`${arr.length} exercício${arr.length===1?'':'s'} · ${Object.keys(state.eduOverrides).length} personalizadas`;$('eduList').innerHTML=arr.slice(0,140).map(ex=>{const p=eduProfile(ex),custom=!!state.eduOverrides[canon(ex)];return`<div class="eduCard"><div class="eduCardHead"><div><b>${esc(ex.name)}</b><div class="small">${esc(ex.family||'')} · ${esc(ex.position||'')} · ${esc(ex.pattern||'')}</div></div><span class="eduCount ${custom?'custom':''}">${custom?'PERSONALIZADA':'AUTO'}</span></div><div class="eduCue"><b>Cue:</b> ${esc(p.cue)}</div><div class="row" style="margin-top:7px"><button class="btn primary" onclick="openEduById('${ex.id}')">ABRIR BLUE EDU</button></div></div>`}).join('')||'<div class="card note">Sem resultados.</div>'}
window.openEduById=function(id){const ex=APP.exercises.find(x=>x.id===id);if(ex)openEdu(ex)}

function diagnose(c){
 const flat=c.blocks.flatMap(b=>b.exercises),w=[];let score=12,last=-1,reg=0;
 flat.forEach(ex=>{const r=positionRank(ex.position);if(last>=0&&r<last-1)reg++;last=r});
 if(c.config.flowRule!=='Livre'&&reg){w.push(`${reg} transição(ões) com subida de nível potencialmente pouco fluida(s).`);score-=Math.min(2,reg)}
 const hands=flat.filter(x=>/plank|bear|crab|press up|mountain|wrist/i.test(x.name+' '+x.position)).length;if(hands>=6){w.push('Densidade elevada de apoio de mãos/punhos.');score--}
 const jumps=flat.filter(x=>/jump|burpee|skater/i.test(x.name)).length;if(c.config.impact==='Baixo'&&jumps){w.push('Impacto acima do filtro escolhido.');score-=2}
 return{score:Math.max(0,score),warnings:w}
}
function finishClass(){currentClass.diagnostic=diagnose(currentClass);renderBuild();renderFollow()}
function exerciseCard(ex,bi,ei){
 const d=document.createElement('div');d.className='exercise';const img=imageMarkup(ex);
 d.innerHTML=`<div class="head"><div class="exname">${bi+1}.${ei+1} — ${esc(ex.name)}</div><div class="timepill" contenteditable="true">${esc(ex.time||'')}</div></div><div class="exgrid"><div><div class="imgbox">${img}</div><input class="file" type="file" accept="image/*" multiple style="display:none"></div><div><div class="meta"><span class="tag">${esc(ex.position||'')}</span><span class="tag">${esc(posLabel(ex.position||''))}</span><span class="tag">${esc(ex.pattern||'')}</span></div><div class="note" style="margin-top:6px"><b>Objetivo:</b> ${esc(ex.objective||'')}</div><div class="eduWhy"><b>Porque está aqui:</b> ${esc(eduContextWhy(ex,bi,ei))}</div><textarea class="notes" placeholder="Cues / regressão / progressão...">${esc(ex.userNotes||'')}</textarea><div class="controls"><button class="btn up">↑</button><button class="btn down">↓</button><button class="btn replace">TROCAR</button><button class="btn edu">BLUE EDU</button><button class="btn image">IMAGENS ${visualImagesFor(ex).length}/2</button><button class="btn fav">${state.prefs.favorite[canon(ex)]?'★ FAVORITO':'☆ FAVORITO'}</button><button class="btn bad avoid">${state.prefs.avoid[canon(ex)]?'EVITAR ✓':'EVITAR'}</button></div></div></div>`;
 const arr=currentClass.blocks[bi].exercises;
 d.querySelector('.up').onclick=()=>move(bi,ei,-1);d.querySelector('.down').onclick=()=>move(bi,ei,1);d.querySelector('.replace').onclick=()=>replaceEx(bi,ei);d.querySelector('.edu').onclick=()=>openEdu(ex,bi,ei);
 d.querySelector('.timepill').onblur=e=>{arr[ei].time=e.target.textContent.trim();renderFollow()};d.querySelector('.notes').oninput=e=>{arr[ei].userNotes=e.target.value;renderFollow()};
 d.querySelector('.fav').onclick=()=>{const c=canon(ex);if(state.prefs.favorite[c])delete state.prefs.favorite[c];else{state.prefs.favorite[c]=true;delete state.prefs.avoid[c]}saveState();renderBuild()};
 d.querySelector('.avoid').onclick=()=>{const c=canon(ex);if(state.prefs.avoid[c])delete state.prefs.avoid[c];else{state.prefs.avoid[c]=true;delete state.prefs.favorite[c]}saveState();renderBuild()};
 const input=d.querySelector('.file');d.querySelector('.image').onclick=()=>input.click();input.onchange=async e=>{await saveFilesToExercise(ex,e.target.files);input.value=''};
 return d
}
function move(bi,ei,dir){const a=currentClass.blocks[bi].exercises,ni=ei+dir;if(ni<0||ni>=a.length)return;[a[ei],a[ni]]=[a[ni],a[ei]];finishClass()}
function replaceEx(bi,ei){const cur=currentClass.blocks[bi].exercises[ei],term=prompt('Procurar exercício:',cur.region||cur.pattern||cur.name);if(term===null)return;const p=filterPool(poolForType(currentClass.config.classType),currentClass.config).filter(x=>lower([x.name,x.region,x.pattern,x.position].join(' ')).includes(lower(term))).slice(0,20);if(!p.length)return alert('Sem resultados.');const txt=p.map((x,i)=>`${i+1}) ${x.name} — ${x.position}`).join('\n');const n=+prompt(txt,'1')-1;if(!p[n])return;const x=JSON.parse(JSON.stringify(p[n]));x.time=cur.time;currentClass.blocks[bi].exercises[ei]=x;finishClass()}
function renderBuild(){
 if(!currentClass){$('blocks').innerHTML='<div class="card note">Ainda não criaste uma aula.</div>';$('sourceBox').innerHTML='';$('diagnostic').innerHTML='';return}
 $('bType').textContent=currentClass.config.classType;$('bMin').textContent=currentClass.config.duration+' min';$('bBlocks').textContent=currentClass.blocks.length;$('bEx').textContent=currentClass.blocks.reduce((a,b)=>a+b.exercises.length,0);$('bScore').textContent=currentClass.diagnostic.score+'/12';
 $('sourceBox').innerHTML=currentClass.video?`<div class="goodbox"><b>${esc(currentClass.mode)}</b><br>${esc(currentClass.video.creator)} — ${esc(currentClass.video.title)}<br><span class="small">${currentClass.video.status==='EXTRAÍDO'?'Exercícios extraídos da fonte. Em modo BLUE, a parte fundamental é reorganizada por nível e o aquecimento/retorno são ajustados para manter fluidez; em modo original, preserva a sequência da fonte.':'Adaptação baseada no perfil do vídeo, não transcrição exata.'}</span></div>`:`<div class="goodbox"><b>Aula criada de raiz</b> a partir dos teus filtros e biblioteca BLUE.</div>`;
 $('diagnostic').innerHTML=currentClass.diagnostic.warnings.length?`<div class="warnbox"><b>Diagnóstico</b><br>${currentClass.diagnostic.warnings.map(x=>'• '+esc(x)).join('<br>')}</div>`:`<div class="goodbox"><b>Diagnóstico</b> Sem alertas relevantes.</div>`;
 const root=$('blocks');root.innerHTML='';currentClass.blocks.forEach((b,bi)=>{const w=document.createElement('div');w.className='block';w.innerHTML=`<div class="blocktitle"><b>${bi+1}. ${esc(b.phase)}</b><span class="chip">${b.minutes} min</span></div>`;b.exercises.forEach((ex,ei)=>w.appendChild(exerciseCard(ex,bi,ei)));root.appendChild(w)})
}
function flat(){const a=[];if(!currentClass)return a;currentClass.blocks.forEach((b,bi)=>b.exercises.forEach((ex,ei)=>a.push({...ex,__block:b.phase,__num:`${bi+1}.${ei+1}`})));return a}
function renderFollow(){
 const xs=flat();if(!xs.length){$('scrollMode').innerHTML='<div class="card note">Sem aula.</div>';return}
 $('scrollMode').innerHTML=xs.map((ex,i)=>`<div class="exercise"><div class="head"><div class="exname">${esc(ex.__num)} — ${esc(ex.name)}</div><div class="timepill">${esc(ex.time||'')}</div></div><div class="exgrid"><div><div class="imgbox">${imageMarkup(ex)}</div></div><div><div class="counter">${i+1} / ${xs.length}</div><div class="meta" style="margin-top:6px"><span class="tag">${esc(ex.__block)}</span><span class="tag">${esc(posLabel(ex.position||''))}</span></div><div class="note" style="margin-top:7px"><b>${esc(ex.objective||'')}</b>${ex.userNotes?'<br>'+esc(ex.userNotes):''}</div></div></div></div>`).join('');
 currentStep=Math.min(currentStep,xs.length-1);renderStep()
}
function renderStep(){const xs=flat();if(!xs.length)return;const ex=xs[currentStep];$('stepCounter').textContent=`${currentStep+1} / ${xs.length}`;$('stepTitle').textContent=ex.name;$('stepSub').textContent=`${ex.__num} · ${ex.__block} · ${ex.time||''}`;$('stepImg').innerHTML=imageMarkup(ex,true);$('stepMeta').innerHTML=`<span class="chip">${esc(posLabel(ex.position||''))}</span><span class="chip">${esc(ex.pattern||'')}</span>`;$('stepNotes').innerHTML=`<b>Objetivo:</b> ${esc(ex.objective||'')}${ex.userNotes?'<br><b>Notas:</b> '+esc(ex.userNotes):''}`}

function fillVisualFamilies(){const sel=$('visualFamily');if(!sel)return;[...new Set(APP.exercises.map(x=>x.family).filter(Boolean))].sort().forEach(v=>{const o=document.createElement('option');o.value=v;o.textContent=v;sel.appendChild(o)})}
function visualSlot(ex,idx){const imgs=visualCache[canon(ex)]||[],src=imgs[idx]||'';return`<div><div class="visualSlot">${src?`<img src="${src}" alt="">`:`<div class="slotEmpty">IMAGEM ${idx+1}<br><small>PNG/JPG/WebP</small></div>`}</div><div class="visualActions"><button class="btn" onclick="chooseVisualSlot('${ex.id}',${idx})">${src?'ALTERAR':'ADICIONAR'} ${idx+1}</button>${src?`<button class="btn bad" onclick="removeVisualSlot('${ex.id}',${idx})">REMOVER</button>`:''}</div></div>`}
function renderVisualLibrary(){if(!$('visualList'))return;const q=lower($('visualSearch').value),fam=$('visualFamily').value;let arr=APP.exercises.filter(ex=>(!fam||ex.family===fam)&&(!q||lower([ex.name,ex.region,ex.pattern,ex.family].join(' ')).includes(q)));const seen=new Set();arr=arr.filter(ex=>{const c=canon(ex);if(seen.has(c))return false;seen.add(c);return true}).slice(0,120);$('visualList').innerHTML=arr.map(ex=>{const n=(visualCache[canon(ex)]||[]).filter(Boolean).length;return`<div class="visualCard"><div class="visualTitle"><div><b>${esc(ex.name)}</b><div class="small">${esc(ex.family)} · ${esc(ex.position||'')} · ${esc(ex.pattern||'')}</div></div><span class="visualCount">${n}/2</span></div><div class="visualSlots">${visualSlot(ex,0)}${visualSlot(ex,1)}</div></div>`}).join('')||'<div class="card note">Sem resultados.</div>'}
window.chooseVisualSlot=function(id,idx){const ex=APP.exercises.find(x=>x.id===id);if(!ex)return;const input=document.createElement('input');input.type='file';input.accept='image/*';input.onchange=async()=>{const f=input.files?.[0];if(!f)return;const data=await fileToCompressedDataURL(f);const c=canon(ex),arr=(visualCache[c]||[]).slice(0,2);arr[idx]=data;await putVisual(c,arr);renderVisualLibrary();renderBuild();renderFollow()};input.click()}
window.removeVisualSlot=async function(id,idx){const ex=APP.exercises.find(x=>x.id===id);if(!ex)return;const c=canon(ex),arr=(visualCache[c]||[]).slice(0,2);arr[idx]='';while(arr.length&& !arr[arr.length-1])arr.pop();if(arr.some(Boolean))await putVisual(c,arr);else await deleteVisual(c);renderVisualLibrary();renderBuild();renderFollow()}
async function exportFullBackup(){await loadVisualCache();const payload={backupVersion:'9.0',createdAt:new Date().toISOString(),state,visualImages:visualCache};const blob=new Blob([JSON.stringify(payload)],{type:'application/json'}),u=URL.createObjectURL(blob),a=document.createElement('a');a.href=u;a.download='BLUE_SYMBIOTE_v9_BACKUP_COMPLETO.json';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(u),1000)}
async function exportStandaloneHtml(){await loadVisualCache();const clone=document.documentElement.cloneNode(true);['eduList','visualList','blocks','scrollMode','historyList','suggestions','diagnostic','sourceBox'].forEach(id=>{const el=clone.querySelector('#'+id);if(el)el.innerHTML=''});const modal=clone.querySelector('#feedbackModal');if(modal)modal.style.display='none';const eduModal=clone.querySelector('